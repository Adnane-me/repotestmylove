name: Push to Khadija Repo
on:
  push:
    branches:
      - main  # change si ta branche est différente

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - run: |
          git config --global user.email "Adnane.Mehdaoui@usmba.ac.ma"
          git config --global user.name "Adnane-me"

          git remote add khadija https://adnane:${{ secrets.TEST }}@github.com/khadijaelmaaroufi/testadnankhadija.git
          name: Push to Khadija Repo
on:
  push:
    branches:
      - main  # change si ta branche est différente

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - run: |
          git config --global user.email "Adnane.Mehdaoui@usmba.ac.ma"
          git config --global user.name "Adnane-me"

          git remote add khadija https://adnane:${{ secrets.TEST }}@github.com/khadijaelmaaroufi/testadnankhadija.git
          git push -u khadija main --force
